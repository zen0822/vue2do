include ../../mixin.pug

div
  article.example-article
    +section('start', '开始使用')
      z-omit(
        :line='4'
        :style='{width: "300px"}'
      )
        | 在中国，404 错误有了更多延伸的意义，它可能是网页不存在，
        | 可能是因为内容非法，也可能是因为内容创作者主动删去。不过，
        | 无论中外，最起码都形成了一个共识：
        |「网络技术，由人类创造，受人类控制，并非完全可靠。」
      z-code.
        #{'<'}z-omit :line="4" :style="{width: '300px'}"#{'>'}
          在中国，404 错误有了更多延伸的意义，它可能是网页不存在，
          可能是因为内容非法，也可能是因为内容创作者主动删去。不过，
          无论中外，最起码都形成了一个共识：
          「网络技术，由人类创造，受人类控制，并非完全可靠。」
        #{'</z-omit>'}
    +section('props', 'props 数据类型')
      z-table(
          border='row'
          auto
          :pageSize='10')
        template(
            slot='thead'
            v-for='(item, index) in ["名字", "类型", "可选值", "说明"]')
          z-table-col(:max-width='index === 3 ? "23px" : ""') {{ item }}
        z-table-row(slot='1')
          z-table-col line
          z-table-col Number
          z-table-col -
          z-table-col 多行省略规定的行数，默认是 1 行