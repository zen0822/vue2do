div.page-build
  article.example-article
    p 构建单页应用（spa）和多页应用（mpa）
    p 全局安装 vue2do
    z-code(type='shell').
      npm i vue2do -g
    p 初始化应用项目
    z-code(type='shell').
      vue2do init project [projectName] // projectName: 项目名字
    p 构建应用
    z-code(type='shell').
      cd [projectName] // 初始化的项目应用目录下
      vue2do build #{'<appType>'} [appName]